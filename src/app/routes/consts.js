const rOnboarding = '/welcome';
const rAccount = '/account';
const rImport = '/import';
const rUser = '/user';

const RouterApp = {
    rOnboarding,
    rAccount,
    rImport,
    rUser,
};

export default RouterApp;
